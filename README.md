## php-CRUD:

PHP Web interface to CRUD any table in DB.

### How-to:
  1. Extract in ```htdocs```.
  2. Update DB access **configurations** in ```con.php```
  3. Update **TableName** in ```index.php```
  4. Open in browser and **Enjoy** easy CRUD!